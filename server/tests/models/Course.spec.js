describe('Course Model', function() {
  it('should create new courses', (done) => {

    done();
  });

  it('should successfully lookup existing courses', (done) => {

    done();
  });

  it('should update existing courses', (done) => {

    done();
  });

  it('should remove courses from the database', (done) => {

    done();
  });
});
