import { chat } from '../../../server/controllers/redisControllers/chat.js';

describe('redisChat Controller', () => {
  it('should return greater than one if message stored in redis', (done) => {
    done();
  });

  it('should return all of the users messages in the redis db', (done) => {
    done();
  });

  it('should invoke saveChats in mysql chat controller after 50 msgs per user', (done) => {
    done();
  });
});