import { chat } from '../../../server/controllers/chat.js';

describe('redisChat Controller', () => {
  it('should return list messages if they are successfully put into db', (done) => {
    done();
  });
});
