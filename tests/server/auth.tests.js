// This passport import in auth.js causes an error in the test suite.
// import Auth from './../../server/controllers/auth.js';

describe('User login and authentication', () => {
  it('should allow valid user credentials to log in', () => {

  });

  it('should prohibit invalid user credentials from logging in', () => {

  });
});
